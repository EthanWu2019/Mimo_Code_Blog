
require('dotenv').config({ path: '/Users/ethanwu/workspace/Mimo_Code_Blog/.env' });
const { PrismaClient } = require('@prisma/client');
const { PrismaPg } = require('@prisma/adapter-pg');
const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL });
const prisma = new PrismaClient({ adapter });

// Build slug from title: lower case, replace non-alphanumeric with dash
function slugify(t) {
  return t
    .toLowerCase()
    .replace(/[^a-z0-9 -]+/g, '')  // strip punctuation
    .replace(/\s+/g, '-')         // spaces to dash
    .replace(/-+/g, '-')            // collapse dashes
    .replace(/^-|-$/g, '');         // trim leading/trailing dash
}

const POSTS = [{"title": "Reframing the React Component Lifecycle", "excerpt": "Why the old useEffect-first mental model is hurting your codebase \u2014 and what to use instead.", "tags": ["react", "typescript"], "content_lines": ["## Why useEffect isn't a default", "", "For years, \"React component\" and \"useEffect\" went hand in hand. Every counter had a useEffect. Every data fetch used one. Every \"scroll when visible\" used one. We have been treating effects as the default tool for side-effects, when in practice most of those side-effects are really **derived state** or **event handlers** that never needed an effect at all.", "", "## The three questions", "", "When you reach for useEffect, ask:", "", "1. **Is this a state transition?** If yes, derive it in the render pass. No effect, no setState, no second render.", "2. **Is this a one-shot sync with an external system?** If yes, an effect is the right tool \u2014 but log the dependency array and the cleanup. Every effect should answer: what cancels me?", "3. **Is this just a delayed user action?** If yes, do the work in the event handler. useEffect-with-timeout is a code smell.", "", "## The cost of the wrong choice", "", "A useEffect with a missing dependency commits the React linter is gaze. A useEffect with the right dependency runs a second render that the user sees. A useEffect with too many dependencies runs a third. A useEffect inside a render that mutates state is a render loop.", "", "When you replace that effect with a derived value, the second render disappears. The user sees the right thing on the first render. The linter stays quiet. The runtime stays quiet. The codebase stays clean.", "", "## Where effects still belong", "", "Network requests, subscriptions, focus management, and the animation-primitive-to-attribute bridge. Anything where this is the side of the wall my code lives on, and the wall has its own clock. For everything else, prefer the event handler or the render pass."]}, {"title": "Server Components Are an Architecture, Not a Feature", "excerpt": "Half the teams adopting RSC are using them as a deployment target. The other half are discovering that the real win is the boundary it draws.", "tags": ["nextjs", "react", "architecture"], "content_lines": ["## The headline got it right by accident", "", "\"Server Components\" sounds like a rendering technique. It is, in the same way that \"the internet\" is a rendering technique \u2014 which is to say, technically true and practically useless. The interesting thing about RSC is not that the server renders the component. It is that the boundary between \"server\" and \"client\" now lives inside your component tree, not at the edge of your network request.", "", "## A boundary you can think about", "", "Before RSC, your architecture diagram was a wall. Server on one side, client on the other. The wall was the network. You drew boxes around \"screens\" and boxes around \"endpoints\" and you tried not to think about the boxes on the other side of the wall.", "", "After RSC, the wall is inside the screen. The boundary is in your component tree. You draw a \"use server\" line and a \"use client\" line on the same page. The component above the line is a synchronous server function. The component below it is a hydrated island. They share types. They share a router. They share a layout.", "", "That sounds like a small change. It is not. When the boundary lives inside the screen, you stop optimising for \"ship the page\" and start optimising for \"ship the right data to the right boundary.\" That is a fundamentally different design problem.", "", "## The mistake I keep seeing", "", "Teams adopt RSC, then put all their state in client components anyway, because that is where useState feels comfortable. They end up shipping the entire component tree to the browser, and the only thing the server-rendering bought them is a slightly faster first paint.", "", "The right move is the other way around: default to server. Push the \"use client\" boundary as far down the tree as it will go. The islands that need state, effect, or browser API get the directive; everything else stays on the server, where the data is.", "", "That is the architectural shift. Everything else is a small change in the same direction."]}, {"title": "Animations That Earn Their Cost", "excerpt": "The difference between a delightful interface and a slow one is whether the motion is doing work the user already wanted to do.", "tags": ["css", "design"], "content_lines": ["## Motion is a signal, not a reward", "", "The question to ask before adding any animation is: what signal does this motion carry that the static version did not? If the answer is none, you have a decorative animation. Decorative animations are fine in marketing pages and brand intros. They are usually a tax in product UI.", "", "The good ones tell the user something they would otherwise have to infer:", "", "- A list item animates *out* of place, not *down*. The user knows it was removed, not sorted.", "- A button briefly grows when pressed. The user feels the click landed.", "- A panel slides from the side it came from. The user knows where the new content came from in the layout.", "", "## The cost that hides", "", "Animations cost frame budget. A 200-millisecond transition that runs across sixty elements on every state change will out-crawl your backend before lunch. The fix is not to shorten the duration \u2014 it is to ensure the animation only runs when the user has reason to see it.", "", "Concretely:", "", "1. **Animate the thing that changed, not the things that didn't.** List items that are not moving should not re-paint because their neighbours did.", "2. **Respect the system preference.** If the user has reduced motion enabled, replace the slide with a fade.", "3. **End the animation on transform and opacity.** Layout properties trigger reflow. Transform and opacity don't. The browser will hand you the GPU. Use it.", "", "## The honest test", "", "Open the page, then close your eyes. Open them. Does the screen make sense? If yes, you did not need the animation. If no \u2014 if the static frame is missing a state that the animation was carrying \u2014 then the animation is doing real work and you should keep it.", "", "Most \"delightful\" interactions I have seen are delightful only because they paper over a missing affordance. The animation goes away when the affordance is added. The affordance is the real fix. The animation is the receipt."]}, {"title": "On the Awkwardness of Multi-Agent Coding", "excerpt": "When the second agent disagrees with the first, you are the diff. Some notes on staying sane while the agents argue.", "tags": ["ai", "architecture"], "content_lines": ["## The setup", "", "I run one agent to plan, a second to implement, a third to review. When they agree, the work goes fast. When they disagree, I have to read three conflicting accounts of what should happen next, and pick the one I will live with.", "", "That picking is the job. The agents are not going to do it for me.", "", "## The failure mode I keep hitting", "", "I ask the planner to break a feature into tasks. The planner writes them out. I ask the implementer to take the first task. The implementer reads the plan, decides the plan is too granular, picks the first three tasks and merges them, and starts writing. By the time the reviewer reads the code, the plan is already irrelevant \u2014 the code exists.", "", "The reviewer can flag this. The reviewer cannot undo it. The implementer is already deep in the third file, and revert to the plan is now a one-hour detour.", "", "## What I do about it", "", "I write the plan into a file the implementer has to read out loud. I put a checkpoint in the code where the implementer has to commit before continuing. I make the planner and the implementer disagree out loud so I can see the disagreement, not just the resolution.", "", "The agents are good at writing code and bad at making decisions that involve the user. I want them to surface the disagreement, not paper over it.", "", "## The cost of being the diff", "", "Every disagreement I resolve is a decision I am not making in code. The decision goes in a comment, or in a commit message, or in a follow-up issue. The decision is also a debt: if I am wrong, the next person on this code reads the comment, reads the code, and has to guess whether the comment is still right.", "", "So I try to write the comment as the *kind* of decision it is, not just the *content* of the decision. \"We chose X because Y\" is more useful in two years than \"We chose X.\" The \"because\" is the part that ages well. The \"X\" is the part that ages out.", "", "## When the agents are right", "", "Most of the time. They read code I have not read in months. They catch the API I forgot to update. They notice the type that drifted out of sync. The agents are good at this.", "", "The agents are bad at: deciding what the product should do. What the user is asking for. What the trade-off is between two valid-looking fixes. The architecture choice. The naming. The boundary between two modules. The user is actual problem.", "", "These are still mine. The agents are tools for executing on those decisions once they are made. They are not the thing that makes the decisions.", "", "That is the part I keep forgetting."]}, {"title": "TypeScript Without the Cult", "excerpt": "Types are a tax you pay up front for savings later. Most teams overpay. Some pay nothing at all. Where the line actually is.", "tags": ["typescript", "architecture"], "content_lines": ["## The promise", "", "TypeScript was sold on a specific story: write your types once, get your editor to fill in the rest, ship fewer bugs. The pitch worked. Most of the new code I read is in TypeScript. The promise is real.", "", "The promise is also partial. The types are a tax. They are an up-front cost you pay so that downstream savings appear. The tax is not free. The savings are not automatic. Most teams overpay, and some teams pay nothing at all, and both teams are wrong.", "", "## Where the tax bites", "", "The most common way I see teams overpay is the **generic-shaped type**. A function that returns Promise of unknown because someone could not be bothered to write the return type. A component that accepts Record of string to any because props vary by use case. An event handler with type of Event because narrowing the type is fifteen extra lines of code that nobody wanted to write.", "", "Each of those types is a tax. The cost is the same as a precise type \u2014 a future contributor has to read past it. The benefit is zero: when the type is any or unknown, the editor can not help, the compiler can not help, and the runtime still has to handle the actual shape. The any type is a tax with no benefit. Stop writing any.", "", "## Where the tax disappears", "", "The second-most common way I see teams overpay is the **over-narrow type**. A function that accepts object with id string but is documented to accept any object with id string and name string as well. The compiler is happy. The user is unhappy. They get a runtime crash. Over-narrow types are the symmetric error of the loose types, and they are both corrected by writing the type you mean.", "", "The third most common is the **premature generic**. A user-list API that returns array of User when it should return Promise of array of User or ApiError. The first version compiles, ships, and turns every consumer into a runtime check. The second version pushes the error to the type system, where it belongs.", "", "## Where the line is", "", "The line is not at \"any is bad\" or \"always be precise.\" The line is at the boundary between \"the type tells you what you can do\" and \"the type is a guess about what you will need next.\" Most of the time, the right answer is the type that describes the function you are writing today. The \"I might need to support this later\" type is the one that turns into the any you regret.", "", "Write the type. Let the next person read past it. That is the deal."]}, {"title": "Why I Stopped Using a CSS Framework", "excerpt": "Three years of writing utility classes by hand taught me what frameworks were saving me from. Sometimes nothing.", "tags": ["css", "design"], "content_lines": ["## What the framework was doing", "", "The CSS framework I used for years gave me three things: a design system, a naming convention, and a reason to never look at the generated CSS. The design system was the part I was paying for. The naming convention was the part I was resenting. The never look at the generated CSS was the part I was most suspicious of.", "", "The naming convention was where the time went. A button needed base-classes size-classes color-classes state-classes. A new contributor spent a day learning the names. A senior contributor spent a day re-learning them, because the names had shifted in some major version. Every few months, the framework wanted to move a class. Every move was a migration.", "", "## What I do now", "", "I write CSS by hand. I use a small set of design tokens, set as custom properties. I keep the cascade. I use the cascade, on purpose, for the things it is actually good at \u2014 like the fact that a button on a card inherits the card is font without me writing it twice.", "", "The amount of CSS I write per component has gone **down**, not up, since I stopped using a framework. I write less because I do not have to learn the framework is name for what I am trying to express. I write display flex instead of d-flex. I write the actual property, the actual value, the actual rule. The browser reads it. The reviewer reads it. The next person on this code reads it.", "", "## The cost I underestimated", "", "I had assumed the framework was saving me keystrokes. It was saving me **decisions**. Every time I needed to do something visual on a page, the framework was offering me a small set of pre-decided options. That was the actual product. The classes were just the shipping container.", "", "When I removed the framework, the decisions came back. I had to decide what spacing to use, what colors to use, what typography to use, on every component. I had decided to spend that decision budget. I was, in fact, willing to spend it.", "", "Most teams are not. The framework is the right choice for a team where the design decisions are owned by one person and the rest of the team should not be making them. The framework is the wrong choice for a team where the design decisions are made together, and the team is willing to argue about them.", "", "## What I would recommend", "", "Try it for a side project. Do not try it on a production app where three other people are committing in the same codebase. The cost of the framework is real. The benefit of the framework is also real. The two are different for different teams. Pick accordingly."]}, {"title": "A Small Habit That Compounds", "excerpt": "A year of writing one-line commit messages taught me how much code review is really about context-setting.", "tags": ["career", "architecture"], "content_lines": ["## The thing that annoyed me", "", "Reviewing a pull request, I would open the diff, read through the changes, and have no idea why the author made them. The code was fine. The tests were there. The diff was correct. I could approve it, but I would be approving a change whose purpose I did not understand.", "", "I would leave a comment: what is the motivation here? The author would respond with a paragraph. I would learn the motivation. The review would finish. The motivation would evaporate from the conversation history within a week.", "", "## The thing I tried instead", "", "I started writing one-line commit messages that named the motivation. Not the diff \u2014 the diff is the commit body. The motivation. The why the diff is a what.", "", "Examples:", "", "- config: bump cookie TTL to 24h after login issue #42", "- api: cache /search for 5m; user complaint about second-page load", "- docs: clarify backup-restore steps after oncall confusion", "", "None of these are impressive. None of them required writing more than a sentence. But a year later, I can read git log --oneline and remember why each commit happened. The diff is recoverable from git show. The motivation is not.", "", "## What I did not expect", "", "The biggest change was not the commit history. It was the pull request review. After a few months of writing motivational commit messages, the diffs got smaller. The author was already thinking about what the reader needed to know, because the same consideration applied to the commit message. The diff and the message converged.", "", "That is the part of small habits that compound that is hard to manufacture. You can not decide to compound a habit. You can decide to start the habit and let the compounding happen. This one compounded for me over about a year.", "", "## The honest caveat", "", "This works for me. It might not work for you. The cost of a motivational commit message is roughly one sentence per commit. The benefit shows up at a year horizon. If your year is spent on throwaway code, the cost wins. If your year is spent on code that will be alive in two years, the benefit wins.", "", "I work on code that will be alive in two years. So the habit pays. Your mileage will vary."]}];

(async () => {
  const owner = await prisma.user.findFirst({ where: { role: 'admin' } });
  if (!owner) { console.error('no admin user'); process.exit(1); }
  console.log('admin:', owner.email);

  // (Tags created lazily by post.create's connectOrCreate.)

  for (const p of POSTS) {
    const slug = slugify(p.title);
    const tagConnect = p.tags.map(name => ({
      where: { name },
      create: { name },
    }));

    const existing = await prisma.post.findUnique({ where: { slug } });
    if (existing) {
      try {
        await prisma.post.update({
          where: { slug },
          data: {
            title: p.title,
            excerpt: p.excerpt,
            content: p.content_lines.join('\n'),
            published: true,
            tags: { set: tagConnect },
          },
        });
        console.log('  updated:', p.title.slice(0, 50));
      } catch (e) {
        console.error('  FAILED update', p.title.slice(0, 50), e.message);
      }
    } else {
      try {
        await prisma.post.create({
          data: {
            title: p.title,
            slug,
            excerpt: p.excerpt,
            content: p.content_lines.join('\n'),
            published: true,
            authorId: owner.id,
            tags: { connectOrCreate: tagConnect },
          },
        });
        console.log('  created:', p.title.slice(0, 50), '|', slug);
      } catch (e) {
        console.error('  FAILED create', p.title.slice(0, 50), e.message);
        if (e.meta) console.error('     meta:', JSON.stringify(e.meta, null, 2).slice(0, 400));
      }
    }
  }

  // seed comments on first post
  const first = await prisma.post.findFirst({ where: { published: true }, orderBy: { createdAt: 'asc' } });
  if (first) {
    await prisma.comment.upsert({
      where: { id: 'seed-c1' },
      update: {},
      create: { id: 'seed-c1', content: 'This is a great breakdown. The "useEffect = default tool" trap caught me a few years ago — I rewrote a list to use derived state and the code got shorter AND faster.', postId: first.id, authorId: owner.id },
    });
    await prisma.comment.upsert({
      where: { id: 'seed-c2' },
      update: {},
      create: { id: 'seed-c2', content: 'Completely agree on the leading-[1.9]. I switched to leading-7 and never went back. The page reads cleaner and the editorial weight matches the content type.', postId: first.id, authorId: owner.id },
    });
    await prisma.comment.upsert({
      where: { id: 'seed-c3' },
      update: {},
      create: { id: 'seed-c3', content: 'Saved this for the next 1:1.', postId: first.id, authorId: owner.id, pinned: true },
    });
    console.log('  seeded 3 comments on first post');
  }

  await prisma.post.updateMany({ data: { viewCount: 12 } });

  const total = await prisma.post.count();
  console.log('total posts now:', total);
  await prisma.$disconnect();
})()

// override the catch handler to dump
process.on('unhandledRejection', e => { console.error('UNHANDLED', JSON.stringify(e, null, 2)); process.exit(1); });

